export type settingsType = {
    sandbox_mode: boolean,
    payment_via: string,
    client_id: string,
    client_secret: string,
    username: string,
    password: string,
    signature: string,
    ipn_notification_url: string,
    webhook_url: string,
}